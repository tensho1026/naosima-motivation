import { Waves } from 'lucide-react'

export default function Footer() {
  return (
    <footer className="app-footer">
      <Waves size={17} />
      <span>今日の一歩を、直島で暮らす未来へ。</span>
    </footer>
  )
}
